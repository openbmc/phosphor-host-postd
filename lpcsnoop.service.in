[Unit]
Description=LPC Snoop Daemon
After=systemd-modules-load.service

[Service]
Restart=always
ExecStart=@bindir@/snoopd -d "/dev/@SNOOP_DEVICE@" -b "@POST_CODE_BYTES@" -h "@HOST_INSTANCES@"

[Install]
WantedBy=@SYSTEMD_TARGET@
